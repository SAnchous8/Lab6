# Смирнов Александр ИТС-2 Лабораторная №6

# Задание 1

### Текст задачи

Разработать класс с тремя целочисленными полями. Создать конструктор копирования. Разработать метод, вычисляющий максимальную из последних цифр полей. Перегрузить метод ToString() для формирования строки из полей класса. Реализовать дочерний класс (его содержание предложить самостоятельно и описать в решении: какой содержательный смысл имеют поля; реализовать конструкторы; предложить и реализовать 2-3 метода). Протестировать все конструкторы и другие методы базового и дочернего классов.

### Алгоритм решения

1. Создание базового класса ThreeNumbers

Поля: Объявить три приватных целочисленных поля (number1, number2, number3).

Конструкторы:

По умолчанию: Инициализирует поля нулями.
С параметрами: Принимает 3 числа, присваивает их полям.
*Копирования: * Принимает объект того же класса, копирует значения полей из него в текущий объект.
Свойства (Properties): Реализовать get и set для доступа к приватным полям (в коде это Number1, Number2, Number3).

Метод GetMaxLastDigit():

Получить абсолютное значение остатка от деления на 10 для каждого из трех чисел (Math.Abs(num % 10)).
Сравнить три полученные цифры.
Вернуть максимальную.
Метод ToString() / GetInfo(): Сформировать строку, содержащую значения трех полей (в коде используется GetInfo()).

2. Создание дочернего класса Triangle (наследуется от ThreeNumbers)

Содержательный смысл полей: Поля базового класса трактуются как длины сторон треугольника (Сторона A, Сторона B, Сторона C).

Конструкторы:

С параметрами: Вызывает конструктор базового класса (base(a, b, c)). В теле конструктора выполняет валидацию: проверяет, что стороны положительны и выполняется неравенство треугольника (сумма двух любых сторон больше третьей). При нарушении выводит ошибку.
Копирования: Вызывает конструктор копирования базового класса.
Собственные методы (2-3 штуки):

GetPerimeter(): Возвращает сумму трех сторон (Number1 + Number2 + Number3).
GetArea(): Вычисляет площадь по формуле Герона (использует полупериметр).
IsRightTriangle(): Проверяет теорему Пифагора для всех возможных комбинаций сторон, чтобы определить, является ли треугольник прямоугольным.
3. Тестирование в Main

Часть 1: Тест ThreeNumbers

Ввод трех чисел с клавиатуры.

Создание объекта ThreeNumbers.

Вызов GetMaxLastDigit() и GetInfo().

Демонстрация работы конструктора копирования.

Изменение значения через свойство Number1 и повторный вывод максимальной цифры.

Часть 2: Тест Triangle

Ввод длин трех сторон.

Создание объекта Triangle (автоматически срабатывает проверка на корректность).

Вывод информации о треугольнике.

Вызов собственных методов: GetPerimeter(), GetArea(), IsRightTriangle().

Демонстрация полиморфизма/наследования: вызов унаследованного метода GetMaxLastDigit() для сторон треугольника.



### Тестирование
<img width="494" height="691" alt="image" src="https://github.com/user-attachments/assets/3372be4b-93e7-4b95-951e-ead1c9c77b3b" />





# Задание 2

### Текст задачи

LineSegment
double x, double y

(координаты начала и конца отрезка на координатной прямой)

Определить, попадает ли заданное число в отрезок. Результат должен быть типа bool.

Унарные операции:

! - вычислить длину отрезка, результат должен быть типа double;

++ увеличить координаты границ отрезка на 1.

Операции приведения типа:

int (явная) – результатом является целая часть координаты х;

double (неявная) – результатом является координата y.

Бинарные операции:

+int d, целое число (лево- и правосторонняя операция, координаты границ увеличиваются на число d);

< LineSegment, целое число – результат равен true, если целое число попадает в заданный отрезок и false–в противном случае.

### Алгоритм решения

1. Попадаем в метод Main - точку входа программы

2. Выводим заголовок "=== ТЕСТИРОВАНИЕ LineSegment ===" и пустую строку

3. Создаем объект класса LineSegment:
   
   Считываем первую координату с помощью ReadDouble
   
   Считываем вторую координату с помощью ReadDouble
   
   Вызываем конструктор LineSegment с двумя аргументами
   
   Сохраняем объект в переменную s
   
   Выводим созданный отрезок

4. В функции ReadDouble:
   
   Запускаем бесконечный цикл
   
   Выводим приглашение prompt
   
   Считываем строку ввода
   
   Проверяем, пустая ли строка - если пустая, выводим ошибку и продолжаем
   
   Заменяем точку на запятую во введенной строке
   
   Пытаемся преобразовать строку в число с плавающей точкой
   
   Если успешно, возвращаем результат
   
   Если нет, выводим ошибку и продолжаем

5. В конструкторе LineSegment:
   
   Принимаем два числа x1 и x2
   
   Находим минимальное значение - присваиваем startX
   
   Находим максимальное значение - присваиваем endX

6. Вычисляем и выводим длину отрезка с помощью унарного оператора !
   
   Вызываем оператор !s, который возвращает длину (endX - startX)

7. Демонстрируем работу унарного оператора ++:
   
   Выводим текущее состояние отрезка
   
   Выполняем s++, который увеличивает startX и endX на 1
   
   Выводим измененный отрезок

8. Демонстрируем явное и неявное приведение типов:
   
   Выводим (int)s - явное приведение к int, возвращает целую часть startX
   
   Выводим (double)s - неявное приведение к double, возвращает значение endX

9. Демонстрируем бинарный оператор + с целым числом:
   
   Считываем целое число d с помощью ReadInt
   
   Выводим результат s + d (левосторонний оператор)
   
   Выводим результат d + s (правосторонний оператор)
   
   В обоих случаях создается новый отрезок со смещенными границами

10. В операторах +:
    
    Левосторонний: принимает отрезок и целое число, возвращает новый отрезок с startX+d и endX+d
    
    Правосторонний: принимает целое число и отрезок, вызывает левосторонний оператор

11. Демонстрируем операторы < и >:
    
    Считываем целое число test с помощью ReadInt
    
    Выводим результат s < test - проверяет, входит ли число в отрезок
    
    Выводим результат s > test - возвращает отрицание от s < test
    
    Выводим текстовое пояснение: принадлежит число отрезку или нет

12. В операторе <:
    
    Вызываем метод Contains, который проверяет, находится ли значение между startX и endX включительно

13. Выводим сообщение "Нажмите любую клавишу..." и ожидаем нажатия клавиши

14. Завершаем программу

### Тестирование
<img width="360" height="508" alt="image" src="https://github.com/user-attachments/assets/b63fbedd-292c-4749-a141-7022fbbec9d3" />



